const React = require('react')
const Default = require('../default')

function show (data) {
    return (
        <Default>
            <main>
                <h1>{date.place.name}</h1>
            </main>
        </Default>
    )
}

module.exports = show